# -*- mode: python ; coding: utf-8 -*-


a = Analysis(
    ['C:/Users/Administrator/Desktop/随机抽取项目/随机抽取v2.0_Alpha.build-78.1.PY'],
    pathex=[],
    binaries=[],
    datas=[],
    hiddenimports=[],
    hookspath=[],
    hooksconfig={},
    runtime_hooks=[],
    excludes=[],
    noarchive=False,
    optimize=0,
)
pyz = PYZ(a.pure)

exe = EXE(
    pyz,
    a.scripts,
    a.binaries,
    a.datas,
    [],
    name='随机抽取v2.0_Alpha.build-78.1',
    debug=False,
    bootloader_ignore_signals=False,
    strip=False,
    upx=True,
    upx_exclude=[],
    runtime_tmpdir=None,
    console=True,
    disable_windowed_traceback=False,
    argv_emulation=False,
    target_arch=None,
    codesign_identity=None,
    entitlements_file=None,
    version='version.txt',
    icon=['C:\\Users\\Administrator\\Desktop\\随机抽取项目\\随机抽取.ico'],
)
